// Test-runner entry point: delegates to the canonical lib/approval_screen_test.dart
// so `flutter test` discovers the approval screen re-pair tests.
import 'package:auth/approval_screen_test.dart' as approval_screen_tests;

void main() => approval_screen_tests.main();
