// Test-runner entry point: delegates to the canonical lib/format_test.dart
// so `flutter test` discovers the format tests.
import 'package:auth/format_test.dart' as format_tests;

void main() => format_tests.main();
