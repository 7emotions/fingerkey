// Test-runner entry point: delegates to the canonical lib/tls_pin_test.dart
// so `flutter test` discovers the TLS pin tests.
import 'package:auth/tls_pin_test.dart' as tls_pin_tests;

void main() => tls_pin_tests.main();
