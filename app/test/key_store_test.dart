// Test-runner entry point: delegates to the canonical lib/key_store_test.dart
// so `flutter test` discovers the KeyStore clear() tests.
import 'package:auth/key_store_test.dart' as key_store_tests;

void main() => key_store_tests.main();
